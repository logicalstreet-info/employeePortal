<div class="form-group msg-form">
  <%= form_with(model: [@single_group, @message], remote: true, class: "d-flex") do |form| %>
    <%= form.text_field :body, id: 'chat-text', autocomplete: 'off' %>
    <%= form.submit data: {disable_with: false}, class: 'btn btn-primary' %>
  <% end %>
</div>